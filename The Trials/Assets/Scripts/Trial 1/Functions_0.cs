using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class Functions_0 : MonoBehaviour
{
    // ignore the "static" keyword it allows the Test Runner can run the function

    // VISIBILITY: public
    // RETURN TYPE: int (void means no return type)
    // PARAMETERS: two integers named num1 and num2
    public static int AddNumbers(int num1, int num2)
    {
        return num1;
    }



    public static bool IsPlayerDead(int damage)
    {
        int playerHp = 15;

        if (true)
        {
          return true;
        }
    }
}
